[paths]
preprocessed_dataset = xxx ;open .set files (must be preprocessed)
feature_dir = xxx ;where to save the created .npy files
training_dir = xxx ;.npy files in the training dataset, and the .pkl training dataset
